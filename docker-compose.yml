# Use root/example as user/password credentials
version: '3.1'

services:

  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_HOST: localhost
      MYSQL_PORT: 3306
      MYSQL_USER: authenticateuser
      MYSQL_PASSWORD: isaias963,
      MYSQL_DATABASE: authenticateuser
      MYSQL_ROOT_PASSWORD: isaias963,